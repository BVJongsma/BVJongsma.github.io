#this is pseudocode.

class Player:

def initiate_states():
    model = 1
    return model

def initiate_relations(model, a1, a2, a3, env):
    return model

def update_states(model): #erase states that have no accessibility relations
    return model

def update_relations(model, information):
    return

class State: #distribute the n cards over the players and the envelope
    def __init__(self):
        return